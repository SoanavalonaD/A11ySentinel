# pipeline/

Owner: **Lewis**. Python, Google ADK, Playwright, axe-core.

Produces data. Writes to Firestore against `contracts/schema.md`.
Nothing in `web/` is edited from here.

Setup instructions live in the root `README.md`.
